#include <bits/stdc++.h>
using namespace std;


int main() {
	cin.tie(0)->ios::sync_with_stdio(0);
	int sum = 0;
	string a;

	while (100) {
		getline(cin, a);
		cout << a << '\n';

		if (a[0] == ' ' or a[0] == NULL) { break; }
	}
}